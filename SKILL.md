---
name: interview-review
description: "面试复盘：把面试录音转写文本，分析成结构化的 Word 复盘文档。用于校招/社招产品经理、技术等岗位面试后的复盘。当用户分享面试录音转写、说『帮我复盘这场面试』『面试结束了，分析一下』『把这场面试整理成文档』，或想沉淀多场面试的逐题问答对照表时触发。输出每题含『面试官问题(原话)/我的回答(原话)/AI评价/改进版回答/提升点』的表格文档，并支持多场面试累积追加到同一份档案。"
---

# 面试复盘（Interview Review）

把一场面试的录音转写，分析成一份**逐题对照、可累积**的 Word 复盘文档。核心价值不在记录，而在「改进版回答」和「提升点」——把每道没答好的题重写成下次能直接用的版本。

## 工作流

输入是面试录音的**转写文本**（用户会直接贴或给文件路径，通常是「讲话人1/讲话人2」交替的对话）。按以下步骤执行：

### Step 1：理解转写、识别角色与轮次

1. 通读转写全文。**面试官 = 提问方**（通常话语更简短、带问号），**候选人 = 回答方**（讲述经历）。
2. 把转写**切分成一个个完整的「问答回合」**：一个回合 = 面试官的一个问题 + 候选人针对它的全部回答（可能跨多个讲话人片段，要合并到同一回合）。
3. 转写常有识别错误（如「詹姆纳」=截图、「circle」=SQL、「engine/恩情」=Agent）。**先查 `references/transcription-errors.md`** 对照已知错误，没查到的新错误结合上下文还原（不确定的联网查证或问候选人，**不要凭空猜**）。

### Step 1.5：识别反问环节（重要！别漏）

通读时单独标出**反问环节**——面试官说「你有什么想问我的」「你有什么问题吗」之后，角色反转，变成**候选人提问、面试官回答**。这一段：

- 每一轮标 `type: "reverse"`，和普通题（`type: "normal"` 或缺省）区分开。
- 渲染成独立的「三、反问环节」段落，**不混在正文题里**。
- 反问题的字段含义和普通题相反：`q` = **我的提问**，`a` = **面试官回答**。
- 评价要分两侧：既评**我的提问**（问得好不好），也分析**面试官回答**（透露了什么信号）。详见 Step 2 反问题字段。

### Step 2：提取原话、逐题结构化（核心）

**普通题**（面试官问、你答）产出 6 个字段。**反问题**（你问、面试官答）产出 7 个字段（评价两侧）。**`q` 和 `a` 必须用录音原话**——清理口吃重复词（「我我我」「呃」），但**保留全部实质内容，绝不概括缩写**。

#### 普通题字段（type 缺省或 "normal"）

| 字段 | 说明 |
|------|------|
| `no` / `time` / `module` | 题号 / 时间点 / 所属经历模块 |
| `q` | 面试官问题（原话，清理口吃） |
| `a` | 我的回答（原话，清理口吃，保留全部实质内容） |
| `eval` | **AI 评价**：先定档（✅好/🟡可改进/🔴问题），再给具体点评 |
| `better` | **改进版回答**：按「总-分-果」结构重写，下次可直接背 |
| `gain` | **提升点**：改进版相比原回答提升了什么（给可迁移的技巧，不空泛） |

#### 反问题字段（type: "reverse"）

| 字段 | 说明 |
|------|------|
| `q` | **我的提问**（原话） |
| `a` | **面试官回答**（原话） |
| `q_eval` | **对提问的评价**：问得好不好、得不得体 |
| `q_better` | **提问改进版**：这个问题怎么问更好 |
| `q_gain` | **提问提升点**：反问技巧的可迁移心得 |
| `a_eval` | **对面试官回答的分析**：透露了什么信息/信号 |
| `a_gain` | **回答启示**：这条反馈对候选人有什么用 |

⚠️ 评价标准、改进版写法、提升点维度、反问环节评价规范——**必须先读 `references/methodology.md`**，严格按其规范执行。这是整个复盘质量的保证。

### Step 3：补全场次元信息

从转写和用户简历/对话中提取：

- `candidate` / `company` / `position` / `date` / `round` / `result`
- `jd`（岗位职责/要求——若用户之前提供过，从对话历史找；没有则留空让用户补）
- `business_line`（面试官业务线——通常在反问环节候选人问「你们做什么」时出现）
- `match_hint`（候选人与该业务的匹配度提示）
- `interviewer_feedback`（面试官的总结性反馈，常出现在结尾，原话引用）
- `conclusions`（本场复盘结论：核心问题/改进方向/致命失误/闪光点/下一步行动）

### Step 4：生成 Word 文档

调用 `scripts/gen_review.py` 把结构化数据渲染成 Word 文档。

**脚本路径（用绝对路径）**：
```bash
REVIEW_SCRIPTS="<skill_directory>/scripts"
```

**首次创建**（这场是复盘档案的第一场）：
```bash
python "$REVIEW_SCRIPTS/gen_review.py" --session session.json --out 复盘档案.docx
```

**追加**（档案已存在，把这场追加到末尾）：
```bash
python "$REVIEW_SCRIPTS/gen_review.py" --session session.json --out 已有复盘档案.docx --append
```

`session.json` 的结构见 `references/session-schema.md`。生成后用 python-docx 读回验证（表格数 = 题数、每表 5 行 2 列、CJK 字体已设）。

## 关键纪律

1. **原话不缩写**：`q` 和 `a` 是录音原话，评价和改进才有锚点。概括掉就失去复盘意义。
2. **评价要狠要准**：不为好看全打 ✅。🔴🟡 才是复盘价值所在。判定标准见 methodology.md。
3. **提升点要可迁移**：提炼成「下次任何题都能用的表达技巧」（如「把结论前置」「把功能清单讲成解决问题」），比背单个答案有用。
4. **改进版用「总-分-果」**：一句话总结→关键动作→结果。这是面试表达的核心范式，每道改进版都要体现。
5. **⚠️ 改进版不能编内容（诚信优先）**：改进版只优化「怎么说」，不能伪造「说什么」。候选人没做过的方向不声称立志深耕、没掌握的技能不声称精通。真话有三种讲法：编内容（❌）、纯自贬（❌）、诚信+策略（✅）。详细判定和示例见 `methodology.md`「改进版的红线」一节。
5b. **⚠️ 改进版不是润色，是完美回答**：写 better 前必须先判断内容本身有没有错（开放题方向/经历事实/知识准确性），错了就推翻重写，不是在原话上调结构。详细见 `methodology.md`「写 better 之前的前置步骤」。
6. **区分理性诚实与自我贬低**：候选人基于客观限制说「没法做某评估」+ 主动补证据 = 理性诚实（✅，是加分）；无端贬低自己做过的事 = 自我贬低（🔴，减分）。评价前先判断是哪种，不要一刀切。
7. **先读 methodology.md 再动手**：评价/改进/提升点的质量全靠它。

## 资源

- `scripts/gen_review.py` — Word 文档生成器（首建 + 追加两种模式）
- `scripts/split_sessions.py` — 把总档案按场次切分成单场独立小word
- `references/methodology.md` — 评价标准、题目分类与答题框架、总-分-果结构、自我设伤5种伪装、反问环节规范（**必读**）
- `references/session-schema.md` — gen_review.py 输入 JSON 的字段规范（含 type=reverse 反问环节）
- `references/transcription-errors.md` — 录音转写错误对照表（提速用，遇新错误往里补）
